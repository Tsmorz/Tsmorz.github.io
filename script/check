#!/bin/sh
# Build the site and check every internal link — the same check CI runs.
# The actual steps live in Taskfile.yml so they're defined once; this is the
# familiar shortcut that delegates there.

set -e
cd "$(dirname "$0")/.."
exec task check
